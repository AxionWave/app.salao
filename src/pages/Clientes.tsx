import AppShell from '@/components/AppShell';

export default function ClientesPage() {
    return (
        <AppShell>
            <h1 className="text-2xl font-semibold text-slate-900">Clientes</h1>
            <p className="mt-2 text-sm text-slate-500">Clientes do salão. Tela placeholder para o time implementar.</p>
            <p className="mt-4 font-mono text-xs text-slate-400">LYR0000003</p>
        </AppShell>
    );
}
